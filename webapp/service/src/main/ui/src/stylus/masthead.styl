.masthead
    background linear-gradient(top, lighten(mastheadBg, 10%), mastheadBg)
    border-radius 0
    height 50px
    &.navbar-default
        color mastheadColor
        .navbar-brand
            color mastheadColor
            &:hover
                color darken(mastheadColor, 30%)
